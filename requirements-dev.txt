# Development-only dependencies: the test suite, the bundler, and Pillow for
# regenerating the icon. Kept out of requirements.txt so none of it is analysed
# into the app bundle.
-r requirements.txt

httpx2       # required by fastapi.testclient
pytest
pyinstaller
pillow
